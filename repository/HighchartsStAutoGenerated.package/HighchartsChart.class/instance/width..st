as yet unclassified
width: aNumber

	"An explicit width for the chart. By default (when null ) the width is calculated from the offset width of the containing element. Defaults to null . 
	-user"

	configurationOptions at: 'width' put: aNumber